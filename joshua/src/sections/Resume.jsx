
import styled from 'styled-components';

const ResumeWrapper = styled.section`
  padding: 20px;
`;

const Resume = () => {
  return (
    <ResumeWrapper>
      <h2>Resume</h2>
      {/* Add link to resume/linkinkd in */}
    </ResumeWrapper>
  );
};

export default Resume;
